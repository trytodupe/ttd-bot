version: "3"

services:
  nonebot:
    build: .
    ports:
      - "8901:8901"
    env_file:
      - .env.prod
    environment:
      ENVIRONMENT: prod
      HOST: 0.0.0.0
      PORT: 8901
    volumes:
      - ./data:/app/data
      - ./db:/app/db
    restart: always